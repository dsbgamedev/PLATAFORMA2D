/// @description Insert description here
// You can write your code in this editor

if(qtd <= 0)
{
	instance_create_layer(0 , 0, layer, obj_transicao, {destino: destino});
	//room_goto(destino);
	//audio_stop_all();
}

//show_debug_message(qtd);
//show_debug_message(fazer);
//fazer = false;
//audio_stop_all();

global.mundos[mundo] = status_bloqueio.desbloqueado;	















